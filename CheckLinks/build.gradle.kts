version = "1.0.9"
description = "Checks links via the VirusTotal api"

aliucord.changelog.set("""
    # 1.0.9
    * added an api key setting to the settings page for the user to input their own api key
    * updated the api url to v3
""".trimIndent())
