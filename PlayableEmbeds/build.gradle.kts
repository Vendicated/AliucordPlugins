version = "1.0.1"
description = "Makes Spotify and Youtube Embeds playable"

aliucord.changelog.set("""
    # 1.0.1
    * Now properly embeds Youtube shorts
    * Now no longer resets embeds when someone reacts
    * Adds 72 new bugs
""".trimIndent())
